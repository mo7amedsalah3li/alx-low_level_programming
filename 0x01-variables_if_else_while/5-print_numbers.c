#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints the alphabet without e,q.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
		int n;

		n = 0;
		while
			(n <= 9)
			{
				putchar(n);
				n++;
			}
		putchar('\n');
		return (0);
}
