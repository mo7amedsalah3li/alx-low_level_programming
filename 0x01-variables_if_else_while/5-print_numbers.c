#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints the alphabet without e,q.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
		int n;

		n = 0;
		while
			(n <= 9)
			{
				printf(n);
				n++;
			}
		printf('\n');
		return (0);
}
